# Shared infrastructure settings
# These values must be consistent across API, Worker, and any other service
# that reads from or writes to Redis.

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
CACHE_KEY_PREFIX=multi-db-connector
